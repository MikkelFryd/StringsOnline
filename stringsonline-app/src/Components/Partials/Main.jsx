import { SideNav } from "../Nav/SideNav"
import { ProductList } from "../Product/ProductList"

export const Main = () => {

    return (
        <main>
            <SideNav />
            <ProductList />
        </main>
    )
}