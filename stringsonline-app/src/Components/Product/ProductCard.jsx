

export const ProductCard = props => {

    return (
        <section>
            <img src="" alt=""/>
            <div>
                <h3>{props.title}</h3>
                <p>{props.description}</p>
            </div>
        </section>
    )
}